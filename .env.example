# PostgreSQL Connection URL
# On Railway, this is provided automatically by adding a PostgreSQL database service
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/spendwise"

# JWT Secret for session cookies (Must be at least 32 characters in production)
AUTH_SECRET="spendwise_super_secret_jwt_encryption_key_2026_production"

# Environment
NODE_ENV="development"
